#  CommonGround – JavaFX Social Connection App

A desktop friendship-matching application built with **JavaFX**, **PostgreSQL**, and **Maven**.

CommonGround allows users to:

-  Create and manage profiles  
-  Connect with other users  
-  Match based on shared interests  
- Administer users and connections  

---

##  Project Structure

```
src/
 └── main/
      ├── java/
      │    └── commonground/
      │         ├── App.java
      │         ├── SceneRouter.java
      │         ├── DbUserRepository.java
      │         ├── UserProfile.java
      │         ├── RoleSelectController.java
      │         ├── AdminController.java
      │         └── UserController.java
      │
      └── resources/
           ├── *.fxml
           └── app.css
```

---

#  How To Run The Project

## Requirements

Before running the project, install:

-  JDK 17  
-  Maven  
-  JavaFX SDK  
-  PostgreSQL  

---

##  Database Setup (PostgreSQL)

Create a database (example):

```
cg_app
```

Run this SQL script:

```sql
CREATE TABLE users (
    id INT PRIMARY KEY,
    name TEXT NOT NULL,
    username TEXT NOT NULL UNIQUE,
    avatar TEXT NOT NULL
);

CREATE TABLE interests (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    label TEXT NOT NULL UNIQUE
);

CREATE TABLE connections (
    user_id INT NOT NULL,
    friend_id INT NOT NULL,
    PRIMARY KEY (user_id, friend_id),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (friend_id) REFERENCES users(id) ON DELETE CASCADE,
    CHECK (user_id < friend_id)
);

CREATE TABLE user_interests (
    user_id INT NOT NULL,
    interest_id INT NOT NULL,
    PRIMARY KEY (user_id, interest_id),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (interest_id) REFERENCES interests(id) ON DELETE CASCADE
);
```

(Optional) Insert test users:

```sql
INSERT INTO users (id, name, username, avatar) VALUES
(1, 'Lucas Bozdog', 'lucas', 'test.png'),
(2, 'Gabi Ionescu', 'gabi', 'test.png');
```

---

##  Configure Database Credentials

Open:

```
DbUserRepository.java
```

Update:

```java
String url = "jdbc:postgresql://localhost:5433/cg_app";
String user = "cg_app";
String password = "lucabozdog";
```

---

##  Run With Maven

Navigate to the folder containing `pom.xml`:

```bash
mvn clean javafx:run
```

---

##  Run In IntelliJ (Recommended)

1. Open project as Maven project  
2. Ensure JDK 17 is selected  
3. If needed, add VM options:

```
--module-path "PATH_TO_JAVAFX/lib" --add-modules javafx.controls,javafx.fxml
```

4. Run:

```
App.java
```

---

#  Architecture Overview

### App.java  
Entry point of the JavaFX application.

### SceneRouter  
Manages navigation between scenes (Role Select, Admin, User).

### DbUserRepository  
Handles all database interactions using JDBC.

### Controllers  
- RoleSelectController  
- AdminController  
- UserController  

Each controller manages UI logic and user interaction.

---

# Features

-  User profile management  
-  Interest-based connections  
-  Add / remove users  
-  Create friendships  
-  Admin panel  

---

#  Important Notes

- PostgreSQL must be running before launching the app.  
- JavaFX is not bundled with Java — make sure it is installed.  
- The project uses relational schema with foreign key constraints.  
- Connection pairs are stored ordered (`user_id < friend_id`) to avoid duplicates.  

---




<img width="1920" height="1020" alt="Screenshot 2026-02-16 194106" src="https://github.com/user-attachments/assets/0fd74018-50f1-4e21-bde0-b1f2a702a0e1" />
<img width="1920" height="1020" alt="Screenshot 2026-02-16 195801" src="https://github.com/user-attachments/assets/de2a966d-40b9-494f-8129-d059efbc0026" />


